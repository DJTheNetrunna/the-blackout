# SERVER config

Canonical inputs:

- `config/sandbox/base.cfg`
- `config/profiles/server.cfg`
- `config/server/KnoxNightmare.ini`
- `mods/manifest.tsv`

`./scripts/generate-config.sh server` renders the active cachedir files.
